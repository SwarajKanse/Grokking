# Starter dependencies — minimum versions only, not hard pins.
# Once the environment is finalized, freeze the actual resolved versions
# (pip freeze) into this file so the run is reproducible from README.md.

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2
rapidfuzz>=3.0        # Levenshtein / Jaro-Winkler / token-set ratio
jellyfish>=1.0         # phonetic encodings (Soundex, NYSIIS, Metaphone)
tqdm>=4.65

# Tier 2 only — uncomment if/when Phase 12 (borderline re-ranking) is
# actually reached, and record the specific checkpoint's name + license
# (must be MIT/Apache-2.0, <=8B params) in Documentation_template.md.
# sentence-transformers>=2.2
